/**
 * Development. The relative prefix is proxied to the .NET API by the dev server
 * (see proxy.conf.json), which keeps the app same-origin and sidesteps CORS.
 */
export const environment = {
  apiUrl: '/api/v1',
};
